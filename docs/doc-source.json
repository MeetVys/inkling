{
  "version": "v0.2",
  "renderer": "doc-skill@v0.2",
  "slug": "2026-05-14-doc-tutorial",
  "title": "HTML Doc Kit — A Friendly Tutorial",
  "created_at": "2026-05-14T18:00:00Z",
  "last_event_at": "2026-05-14T18:00:00Z",
  "output_dir": "docs/2026-05-14-doc-tutorial",
  "theme": "warm-notebook@v0",
  "doc_type_hint": "learning",
  "chassis": "conversation-spine",
  "seed": { "type": "none", "path": null, "sha256": null },

  "events": [
    {
      "n": 1,
      "ts": "2026-05-14T18:00:00Z",
      "verb": "new",
      "instruction": "I have a friend, I want to share with the primary flow of how to work with this tool/skill. Create an html doc that acts as a tutorial for it. Use diagrams, flow charts to explain. And be interactive.",
      "origin": "user",
      "files_touched": ["index.html", "style.css", "runtime.js", "doc-source.json"],
      "assets_added": [],
      "assets_removed": [],
      "structures_used": [],
      "structures_invented": [
        {
          "name": "conversation-spine chassis",
          "shape": "Doc body is laid out as a simulated conversation; user-friend messages on left, agent responses on right with files-touched chips. The form mirrors the function — the tutorial about a conversational tool IS a conversation.",
          "purpose": "Teach the primary user flow by showing it, not describing it. The reader experiences the tool by reading a session."
        },
        {
          "name": "files-touched chips",
          "shape": "Small mono-styled pill tags inside agent turns: ~ file (modified), + file (added). Two color variants for added vs modified.",
          "purpose": "Show exactly which files the agent edited per turn — makes the 'agent edits the whole package' contract visible at a glance."
        },
        {
          "name": "interactive folder anatomy",
          "shape": "320px tree on the left, detail panel on the right. Click any file node to swap the detail panel content: file path, role chip, body prose, bullet list.",
          "purpose": "Make the doc folder's structure tangible. Reader clicks each file and learns what it does in context, not in a wall of prose."
        },
        {
          "name": "compare tabs (md vs doc)",
          "shape": "Two-tab toggle. Markdown tab shows the same content as raw .md; Doc Kit tab shows it rendered with a reactive funnel chart and inline percentages.",
          "purpose": "Make the 'why not markdown' question concrete with a side-by-side, not an abstract argument."
        },
        {
          "name": "try-it 4-card grid",
          "shape": "Two-column grid of four prompt cards. Each card has a step number, a title, a one-line prose, and the exact natural-language prompt to paste into Claude Code.",
          "purpose": "Lower the activation energy. Reader copies prompt #1 into Claude Code and is off."
        },
        {
          "name": "meta event-log footer",
          "shape": "Footer section that fetches and displays the doc's own doc-source.json events. Becomes a live mirror as the doc evolves.",
          "purpose": "Close the loop — the doc that teaches the tool shows how it was itself built. Most direct proof the tool works."
        }
      ],
      "behaviors_used": [],
      "behaviors_invented": [
        {
          "name": "flowStepper",
          "purpose": "Renders an interactive 5-step pipeline from a JSON tag. Each step is a clickable button; activating one swaps an explanation panel and a code-example panel below.",
          "appended_to": "runtime.js"
        },
        {
          "name": "folderAnatomy",
          "purpose": "Wires the folder-tree buttons to swap the detail panel from a JSON tag of file descriptors. Click-to-reveal pattern with aria-pressed state.",
          "appended_to": "runtime.js"
        },
        {
          "name": "toggleCompare",
          "purpose": "Two-tab before/after toggle. Manages aria-pressed on tabs and data-active on panels.",
          "appended_to": "runtime.js"
        },
        {
          "name": "eventLogReveal",
          "purpose": "Fetches the doc's own doc-source.json at runtime and pretty-prints the events[] into a <pre>. The doc reads itself. Falls back gracefully when opened via file:// (CORS).",
          "appended_to": "runtime.js"
        }
      ],
      "event_audit": {
        "originality": "9/10 — chassis itself is invented (conversation-spine), 6 new structural shapes, 4 new behavioral primitives. None of the starter visual vocabulary was reached for as a primary frame."
      },
      "summary": "Created a tutorial doc using a conversation-spine chassis — the page itself dramatises the primary flow it teaches. Invented six structures (chassis, file-chips, anatomy tree, compare tabs, try-it grid, meta event-log footer) and four behaviors (flowStepper, folderAnatomy, toggleCompare, eventLogReveal). The eventLogReveal makes the doc read its own manifest — the tool teaches itself."
    },
    {
      "n": 2,
      "ts": "2026-05-14T22:30:00Z",
      "verb": "edit",
      "instruction": "Update the inkling tutorial doc. Instead of being just a tutorial, make it a proper landing page. Talk about the problem, the solution, keep tutorial and examples. The 'talk to your docs' line is too cute — focus on 'sky is the limit', interactive, aesthetic, computational, soothing to eye and more.",
      "origin": "user",
      "files_touched": ["index.html", "style.css", "doc-source.json"],
      "assets_added": [],
      "assets_removed": [],
      "structures_used": ["hero", "compare", "flow-stepper", "convo", "anatomy", "try-it", "meta-foot"],
      "structures_invented": [
        {
          "name": "problem-grid",
          "shape": "2-column responsive grid of bordered cards. Each card has a serif-display strong label followed by 1-2 prose sentences. Used to name the markdown ceiling in six concrete examples.",
          "purpose": "Make the problem visceral with named scenarios (perf reports, architecture docs, decision walks, learning notes, vision docs, alternatives) instead of abstract complaints."
        },
        {
          "name": "pillars-grid",
          "shape": "5-column responsive grid (5 → 2 → 1 across breakpoints). Each card has a mono-styled number eyebrow, a serif-display H3, and a one-line muted-prose body. Hover lifts and changes border to clay.",
          "purpose": "Make the five-axis contract (Interactive, Computational, Aesthetic, Warm, Originality) the visual centerpiece of the Solution section — readable in 10 seconds, scrollable past in 2."
        },
        {
          "name": "examples-list",
          "shape": "Vertical stack of full-width clickable cards. Each card has a clay-dark serif title and a prose body; hover lifts and recolors the border.",
          "purpose": "Surface the three reference renders explicitly with honest one-line notes per example. Previously the examples were only discoverable via the gallery sub-page and a buried compare reference."
        }
      ],
      "behaviors_used": ["flowStepper", "folderAnatomy", "toggleCompare", "eventLogReveal"],
      "behaviors_invented": [],
      "event_audit": {
        "originality": "8/10 — three new structural shapes invented for the Problem/Solution/Examples sections. No new behavioral primitives this event (existing four primitives cover the new sections). The pillars-grid is the strongest of the three — it makes the contract visible at a glance."
      },
      "summary": "Restructured the page from tutorial-only to proper landing: Hero with 'no ceiling' framing (replacing the too-cute 'talk to your docs'), then Problem section naming six concrete ways markdown caps thinking docs, then Solution section centered on a new five-pillar grid, then existing tutorial sections preserved as 'how it actually works', then an Examples teaser linking to the gallery. Removed the redundant compare section (its content moved into Problem). Invented three structural shapes (problem-grid, pillars-grid, examples-list)."
    },
    {
      "n": 3,
      "ts": "2026-05-14T23:15:00Z",
      "verb": "edit",
      "instruction": "The compare panel illustrates the markdown ceiling but doesn't prove it. Replace the static bar chart on the inkling side with a real interactive funnel — hover tooltip, raw/percent toggle, click-to-explain drawer. The fix should itself BE the proof.",
      "origin": "user",
      "files_touched": ["index.html", "style.css", "runtime.js", "doc-source.json"],
      "assets_added": [],
      "assets_removed": [],
      "structures_used": ["compare"],
      "structures_invented": [
        {
          "name": "funnel chassis",
          "shape": "Container with header (title + sub + toggle pill), bars region (button rows: step label + animated bar + value), explanation drawer (slides in below, max-height transition), italic footer, absolute-positioned tooltip overlay. The whole thing is a self-contained chart-with-context primitive.",
          "purpose": "Replace the static hardcoded bars with a real component that proves the four pillars in one place. The chassis becomes a reusable shape for any funnel/conversion-data doc the kit renders in the future."
        }
      ],
      "behaviors_used": ["toggleCompare"],
      "behaviors_invented": [
        {
          "name": "funnel",
          "purpose": "Reads inline JSON from <script id=\"funnel-data\">, computes percentages from raw counts (first row = baseline), renders SVG-free bars with three layered interactions: hover/focus shows a tooltip with both raw+percent values, the raw/percent toggle re-renders with smooth width transitions, clicking any row opens an explanation drawer with the per-step context. The chart reads its own data — no hardcoded widths, no static labels. Editing the inline JSON updates everything on reload.",
          "appended_to": "runtime.js"
        }
      ],
      "event_audit": {
        "originality": "9/10 — new structural chassis (funnel) AND new behavioral primitive (funnel) invented for this event. Replaces an inert static-bars block that was the page's weakest demo. The interaction set (hover + toggle + click) is non-trivial — none of it existed in the kit before."
      },
      "summary": "Fixed the compare panel's central weakness. The inkling side was promising 'data is bound, not pasted' but was actually rendering hardcoded inline-style widths. Replaced with a real funnel primitive: hover any bar for raw count + conversion %, toggle between raw counts and percentages (computed from data, not hardcoded), click any row for a per-step explanation drawer. The panel now genuinely demonstrates what markdown cannot do — interactive, computational, aesthetic, warm in one visible component. Added matching styles (funnel-head, funnel-toggle, funnel-row, funnel-bar, funnel-drawer, funnel-tip). User caught the gap before posting publicly — the proof-vs-illustration distinction matters when the page is the pitch."
    }
  ],

  "last_event_at": "2026-05-14T23:15:00Z",

  "doc_audit": {
    "interactive": "10/10 — flow stepper, folder anatomy, compare tabs, focus toggle, print, scroll progress, contenteditable hero, three card grids with hover states, plus the new funnel chart (hover tooltip, raw/percent toggle, click-to-explain drawer per row). 25+ distinct interactions across the page.",
    "computational": "9/10 — eventLogReveal fetches the manifest; the new funnel primitive reads inline JSON, computes percentages from raw counts, re-renders on toggle, and resolves per-row explanations from the same data source. The page now computes in three distinct ways instead of one.",
    "aesthetic": "9/10 — warm-notebook tokens; rich typographic hierarchy across hero, problem, solution, tutorial, examples; clay/sage accents used consistently; new pillars-grid and problem-grid land cleanly within the existing palette.",
    "warm": "9/10 — paper tones, Fraunces display, Source Serif body, gentle interactions, generous spacing, the warmth doesn't break across the new sections.",
    "originality": "9/10 — primary chassis invented (conversation-spine), eight structural shapes invented across two events (file-chips, anatomy tree, compare tabs, try-it grid, meta event-log footer, problem-grid, pillars-grid, examples-list), four behavioral primitives invented. Page now serves as both tutorial and landing without templating either job."
  },

  "notes": "First doc rendered under the v0.2 conversational schema; first to receive an /inkling edit event. Validates the conversational contract end to end: event log accepts second event cleanly, structures_invented appended per event, doc_audit recomputed against the full doc state, event_audit captures per-event originality. Two existing v0 docs (2026-05-14-vision, 2026-05-14-doc-explained) are still pending manifest migration; will be done on next op against them."
}
